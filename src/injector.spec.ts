import { Injector } from './injector';

describe('Injector', () => {
  describe('resolve', () => {
    // it('stuff');
  });

  describe('resolveInjectParameters', () => {});

  describe('getInjectMetadataForParam', () => {});

  describe('getInjectMetadata', () => {});

  describe('resolveArray', () => {});

  describe('resolveStringToken', () => {});

  describe('addService', () => {});

  describe('addToken', () => {
    it('should add provider token to tokenProviders map', () => {});
  });
});
